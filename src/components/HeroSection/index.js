import {
  HeroContainer,
  HeroBg,
  // VideoBg,
} from './HeroElements';

const index = () => (
  <HeroContainer id="home">
    <HeroBg>
      {/* <VideoBg autoplay loop muted src={} type="video/mp4" /> */}
    </HeroBg>
  </HeroContainer>
);

export default index;
